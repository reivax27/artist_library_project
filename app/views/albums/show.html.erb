<h2><%= @album.name%> by <%= @album.artist.name %></h2>
<p class="list-group-item list-group-item-info" style="width: 30rem;">There are <%= @album.artist.albums.size %> other albums by <%= @album.artist.name %>.</p>